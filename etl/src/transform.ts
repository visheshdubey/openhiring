export const transformTask = async () => {

    // TODO: 
    /**
     * 1. Get all files from the destination folder
     * 2. Pick only files according to the threshold
     * 3. Call OpenAI Client to transform the message (In batches)
     * 4. Form an Object with Raw & Processed jobs.
     * 5. save in transform container
     */
} 